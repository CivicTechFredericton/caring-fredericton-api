service: caring-fred

provider:
  name: aws
  region: ${{env:AWS_DEFAULT_REGION, 'ca-central-1'}}
  stage: ${{opt:stage}}
  stackName: ${{self:service}}-${{self:provider.stage}}-users
  deploymentBucket:
    name: ${{self:service}}-${{self:provider.stage}}-serverlessdeploymentbucket
    serverSideEncryption: AES256

  # Force serverless variables to be double braced - ${{ }} - so as to
  # not interfere with Fn::Sub
  # See https://serverless.com/framework/docs/providers/aws/guide/variables/#using-custom-variable-syntax
  variableSyntax: "\\${{([ ~:a-zA-Z0-9._@\\'\",\\-\\/\\(\\)]+?)}}"

  runtime: python3.8
  memorySize: 128
  timeout: 6

  lambdaHashingVersion: 20201221

  # Reference external API Gateway definition
  apiGateway:
    restApiId: ${{cf:${{self:service}}-${{self:provider.stage}}-apig.ApiGateway}}
    restApiRootResourceId: ${{cf:${{self:service}}-${{self:provider.stage}}-apig.ApiGatewayRootResourceId}}

#  httpApi:
#    id: ${{cf:${{self:service}}-${{self:provider.stage}}-apig.HttpApi}}

custom:
  user_pool_name: ${{self:service}}-${{self:provider.stage}}-users

resources:
  Resources:
    #===================================================================
    # Cognito
    #===================================================================
    CognitoUserPool:
      Type: AWS::Cognito::UserPool
      DeletionPolicy: Delete
      Properties:
        EmailConfiguration:
          EmailSendingAccount: COGNITO_DEFAULT
          # EmailSendingAccount: DEVELOPER
          # ReplyToEmailAddress: # email address
          # SourceArn: # sourceARN to verified email address in SES
        AdminCreateUserConfig:
          AllowAdminCreateUserOnly: False
          InviteMessageTemplate:
            EmailSubject: Your temporary password
            EmailMessage: |
              Your username is {username} and temporary password is {####}.
        AutoVerifiedAttributes:
          - email
        EmailVerificationMessage: |
          Your verification code is {####}.
        EmailVerificationSubject: |
          Your verification code
        Policies:
          PasswordPolicy:
            RequireLowercase: true
            RequireNumbers: true
            RequireSymbols: false
            RequireUppercase: false
            MinimumLength: 8
        UsernameAttributes:
          - email
          - phone_number
        UserPoolName: ${{self:custom.user_pool_name}}
        UserPoolTags:
            CLIENT: ServUs Health
            PROJECT: Navigate User Portal

    CognitoUserPoolClient:
      Type: AWS::Cognito::UserPoolClient
      Properties:
        ClientName: users
        ExplicitAuthFlows:
          - ADMIN_NO_SRP_AUTH
        RefreshTokenValidity: 30
        UserPoolId:
          Ref: CognitoUserPool

    CognitoAuthorizer:
      Type: AWS::ApiGateway::Authorizer
      Properties:
        IdentitySource: method.request.header.Authorization
        Name: ${{self:service}}-${{self:provider.stage}}-users-authorizer
        RestApiId: ${{self:provider.apiGateway.restApiId}}
        Type: COGNITO_USER_POOLS
        ProviderARNs:
          - Fn::GetAtt: [CognitoUserPool, Arn]

  #===================================================================
  # Stack Outputs
  #===================================================================
  Outputs:
    UserPoolAuthorizer:
      Value:
        Ref: CognitoAuthorizer
      Export:
        Name: !Sub ${{self:service}}-${{self:provider.stage}}:user-pool-authorizer

    UserPoolName:
      Value:
        Fn::Join:
          - ''
          - - ${{self:custom.user_pool_name}}

    UserPoolId:
      Value:
        Ref: CognitoUserPool
      Export:
        Name: !Sub ${{self:service}}-${{self:provider.stage}}:user-pool-id

    UserPoolClientId:
      Value:
        Ref: CognitoUserPoolClient
      Export:
        Name: !Sub ${{self:service}}-${{self:provider.stage}}:user-pool-client-id

    UserPoolArn:
      Value: !GetAtt CognitoUserPool.Arn
      Export:
        Name: !Sub ${{self:service}}-${{self:provider.stage}}:user-pool-arn
